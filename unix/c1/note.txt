系统调用是内核对外提供的接口，被封装成C函数供开发者调用，但系统调用不是库函数。而库函数有可能是借助系统调用实现的。

例如 fork exec是系统调用，而system popen是库函数，在这里，库函数是借助系统调用封装的更复杂的功能

而有的库函数 如 strlen   atoi  等等，则只是库函数，不包含任何系统调用。

开发者可以直接进行系统调用

delete 、ctlr+c发送中断信号  ctdrl+、发送退出信号。系统对这两者的默认处理方式是结束进程。